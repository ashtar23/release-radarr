---
phase: 02-watchlist-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260317190000_create_watchlists.sql
  - supabase/functions/_shared/database.types.ts
  - supabase/functions/api/index.ts
  - supabase/functions/api/routing.ts
  - supabase/functions/api/handlers/watchlist.ts
  - supabase/functions/api/data/watchlist-repository.ts
  - packages/types/src/watchlist.ts
  - packages/types/src/index.ts
  - packages/api-client/src/index.ts
autonomous: true
requirements:
  - WATCH-01
  - WATCH-02
  - WATCH-04
user_setup: []
must_haves:
  truths:
    - Authenticated users can create and delete watchlist entries tied to cached title IDs.
    - Watchlist records are scoped by authenticated user and protected by RLS policies.
    - Shared API client exposes typed watchlist list/add/remove methods for app use.
  artifacts:
    - supabase/migrations/20260317190000_create_watchlists.sql
    - supabase/functions/api/handlers/watchlist.ts
    - supabase/functions/api/data/watchlist-repository.ts
    - packages/types/src/watchlist.ts
    - packages/api-client/src/index.ts
  key_links:
    - routing.ts dispatches /watchlist requests to handlers/watchlist.ts.
    - handlers/watchlist.ts calls watchlist-repository.ts and returns payloads matching @repo/types watchlist contracts.
    - createReleaseRadarApiClient in packages/api-client uses /functions/v1/api/watchlist endpoint paths and returns typed responses.
---

<objective>
Build the backend and shared contract foundation for watchlist persistence and typed client access.
</objective>

# Phase 2 Plan 01: Backend Watchlist Foundation

## Objective
- **What:** Add watchlist persistence (schema + policies), API handlers, and shared types/client methods.
- **Why:** Mobile and web watchlist UI work in Phase 2 depends on a stable backend and shared typed API surface.
- **Output:** Watchlist DB migration, API route handlers, and `@repo/api-client` watchlist methods.

<execution_context>
@/Users/vladimirturkonja/.codex/get-shit-done/workflows/execute-plan.md
@/Users/vladimirturkonja/.codex/get-shit-done/templates/summary.md
</execution_context>

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-watchlist-core/02-CONTEXT.md
@supabase/functions/api/index.ts
@supabase/functions/api/routing.ts
@supabase/functions/_shared/database.types.ts
@packages/types/src/watchlist.ts
@packages/api-client/src/index.ts

## Tasks

<task type="code">
  <name>Create watchlists table + policies migration</name>
  <files>supabase/migrations/20260317190000_create_watchlists.sql, supabase/functions/_shared/database.types.ts</files>
  <read_first>supabase/migrations/20260308143000_create_titles_search_cache.sql, supabase/functions/_shared/database.types.ts, docs/backend.md</read_first>
  <action>Create migration `supabase/migrations/20260317190000_create_watchlists.sql` that adds `public.watchlists` with columns `id text primary key`, `user_id uuid not null`, `title_id text not null references public.titles(id) on delete cascade`, `created_at timestamptz default timezone('utc', now())`, and unique constraint `(user_id, title_id)`. Enable RLS on `public.watchlists` and add policies so authenticated users can select/insert/delete only rows where `auth.uid() = user_id`. Add indexes on `(user_id, created_at desc)` and `(title_id)`. Update `supabase/functions/_shared/database.types.ts` to include typed `watchlists` table Row/Insert/Update definitions that match the migration schema.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `supabase/migrations/20260317190000_create_watchlists.sql` contains `create table if not exists public.watchlists`.
    - `supabase/migrations/20260317190000_create_watchlists.sql` contains `unique (user_id, title_id)`.
    - `supabase/migrations/20260317190000_create_watchlists.sql` contains `alter table public.watchlists enable row level security`.
    - `supabase/functions/_shared/database.types.ts` contains `watchlists: {`.
  </acceptance_criteria>
  <done>Watchlist table + policy migration exists and shared Supabase DB types include the new table.</done>
</task>

<task type="code">
  <name>Add watchlist API routing and handlers</name>
  <files>supabase/functions/api/index.ts, supabase/functions/api/routing.ts, supabase/functions/api/handlers/watchlist.ts, supabase/functions/api/data/watchlist-repository.ts</files>
  <read_first>supabase/functions/api/index.ts, supabase/functions/api/routing.ts, supabase/functions/api/handlers/search.ts, supabase/functions/api/data/titles-repository.ts, docs/backend.md</read_first>
  <action>Extend API routing to recognize `/watchlist` endpoints under `/functions/v1/api`. Update `index.ts` so watchlist routes allow authenticated `GET`, `POST`, and `DELETE` requests while preserving existing search/detail GET behavior. Add `handlers/watchlist.ts` with list/add/remove handlers that read the bearer token user context via Supabase and call a new `data/watchlist-repository.ts`. Repository functions must list watchlist rows joined to title data, insert `(user_id,title_id)` idempotently, and delete by `(user_id,title_id)`. Return errors as JSON with existing `jsonResponse` utility conventions.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `supabase/functions/api/routing.ts` contains route handling for `watchlist`.
    - `supabase/functions/api/handlers/watchlist.ts` exports list/add/remove handler functions.
    - `supabase/functions/api/data/watchlist-repository.ts` contains functions for list, insert/upsert, and delete by `user_id` and `title_id`.
    - `supabase/functions/api/index.ts` contains request handling branch that dispatches watchlist routes.
  </acceptance_criteria>
  <done>Watchlist API endpoints exist and connect route resolution -> handler -> repository.</done>
</task>

<task type="code">
  <name>Add shared watchlist contracts and typed API client methods</name>
  <files>packages/types/src/watchlist.ts, packages/types/src/index.ts, packages/api-client/src/index.ts</files>
  <read_first>packages/types/src/watchlist.ts, packages/types/src/titles.ts, packages/types/src/index.ts, packages/api-client/src/index.ts, packages/config/src/index.ts</read_first>
  <action>Update `packages/types/src/watchlist.ts` with explicit watchlist DTOs for list response and add/remove request payloads while keeping the existing `WatchlistItem` shape aligned with Phase 2 decisions (`title`, cover/name/date data, and releases available for detail needs). Re-export all new watchlist types from `packages/types/src/index.ts`. Extend `createReleaseRadarApiClient` in `packages/api-client/src/index.ts` with `listWatchlist()`, `addWatchlistItem({ titleId })`, and `removeWatchlistItem({ titleId })` methods that call `${API_PATH_PREFIX}/watchlist`, send auth headers the same way as existing methods, and validate payload shape before returning.</action>
  <verify>pnpm check-types</verify>
  <acceptance_criteria>
    - `packages/types/src/index.ts` contains watchlist type exports beyond `WatchlistItem`.
    - `packages/api-client/src/index.ts` contains `listWatchlist(`, `addWatchlistItem(`, and `removeWatchlistItem(`.
    - `packages/api-client/src/index.ts` contains `/watchlist` endpoint usage.
  </acceptance_criteria>
  <done>Apps can call typed shared client methods for watchlist list/add/remove against backend watchlist routes.</done>
</task>

## Verification
- `npx tsc --noEmit`
- `pnpm check-types`
- `pnpm test:backend-contracts` (after adding watchlist assertions or confirming existing suite still passes)

## Success Criteria
- [ ] Watchlist persistence is enforced by DB constraints and RLS.
- [ ] API routes for watchlist list/add/remove are wired under `/functions/v1/api`.
- [ ] Shared contracts and API client methods expose watchlist operations for app layers.
