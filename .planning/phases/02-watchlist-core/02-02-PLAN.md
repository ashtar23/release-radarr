---
phase: 02-watchlist-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/mobile/src/features/watchlist/hooks/use-watchlist-query.ts
  - apps/mobile/src/features/watchlist/hooks/use-watchlist-mutations.ts
  - apps/mobile/src/features/watchlist/components/watchlist-state-view.tsx
  - apps/mobile/src/features/watchlist/components/watchlist-list.tsx
  - apps/mobile/src/app/(tabs)/watchlist/index.tsx
  - apps/mobile/src/app/titles/[titleId].tsx
  - apps/mobile/src/components/profile-header-button.tsx
  - apps/mobile/src/components/pressable-row.tsx
  - apps/mobile/src/lib/api-client.ts
  - apps/mobile/src/app/(modals)/profile.tsx
autonomous: true
requirements:
  - WATCH-01
  - WATCH-02
  - WATCH-03
  - WATCH-04
user_setup: []
must_haves:
  truths:
    - Authenticated mobile users can add/remove watchlist items from title details and remove from watchlist rows.
    - Signed-out mobile users see watchlist entry points that route into the profile modal sign-in flow.
    - Mobile watchlist tab shows a rich list with title name, cover, and primary release date and supports swipe removal.
  artifacts:
    - apps/mobile/src/app/(tabs)/watchlist/index.tsx
    - apps/mobile/src/app/titles/[titleId].tsx
    - apps/mobile/src/features/watchlist/hooks/use-watchlist-query.ts
    - apps/mobile/src/features/watchlist/components/watchlist-list.tsx
  key_links:
    - Title details header action and watchlist tab CTA both route to profile modal when unauthenticated.
    - Watchlist list screen consumes shared API client watchlist hooks and removes items via swipe.
    - Watchlist item rendering applies Phase 2 detail rules (cover + name + primary release date, no addedAt).
---

<objective>
Implement the mobile watchlist user experience and signed-out routing behavior using the Phase 2 decisions.
</objective>

# Phase 2 Plan 02: Mobile Watchlist UX

## Objective
- **What:** Add mobile watchlist query/mutation flows, rich list rendering, swipe removal, and title-details header integration.
- **Why:** Phase 2 requires mobile users to manage and view watchlists with explicit signed-out behavior.
- **Output:** Mobile watchlist feature modules and updated route/screens wired to watchlist backend APIs.

<execution_context>
@/Users/vladimirturkonja/.codex/get-shit-done/workflows/execute-plan.md
@/Users/vladimirturkonja/.codex/get-shit-done/templates/summary.md
</execution_context>

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-watchlist-core/02-CONTEXT.md
@apps/mobile/src/app/titles/[titleId].tsx
@apps/mobile/src/app/(tabs)/watchlist/index.tsx
@apps/mobile/src/app/(modals)/profile.tsx
@apps/mobile/src/components/list-row.tsx
@apps/mobile/src/components/list-section.tsx

## Tasks

<task type="code">
  <name>Add mobile watchlist data hooks and query state</name>
  <files>apps/mobile/src/features/watchlist/hooks/use-watchlist-query.ts, apps/mobile/src/features/watchlist/hooks/use-watchlist-mutations.ts, apps/mobile/src/lib/api-client.ts</files>
  <read_first>apps/mobile/src/features/search/hooks/use-search-titles-query.ts, apps/mobile/src/lib/api-client.ts, packages/api-client/src/index.ts, .planning/phases/02-watchlist-core/02-CONTEXT.md</read_first>
  <action>Create mobile watchlist feature hooks that use React Query and shared API client methods from Plan 02-01. Add `useWatchlistQuery` for authenticated list fetch and mutation hooks for add/remove with optimistic cache updates. Ensure guest mode does not call watchlist endpoints and instead returns a signed-out state marker used by UI screens.</action>
  <verify>pnpm --filter mobile check-types</verify>
  <acceptance_criteria>
    - `apps/mobile/src/features/watchlist/hooks/use-watchlist-query.ts` contains `useQuery(` and returns a signed-out state branch.
    - `apps/mobile/src/features/watchlist/hooks/use-watchlist-mutations.ts` contains both add and remove mutation hooks.
    - Mobile watchlist hooks call shared API methods `listWatchlist`, `addWatchlistItem`, and `removeWatchlistItem`.
  </acceptance_criteria>
  <done>Mobile watchlist data layer is available for both authenticated and guest UI paths.</done>
</task>

<task type="code">
  <name>Implement watchlist tab UX including signed-out CTA and rich list</name>
  <files>apps/mobile/src/app/(tabs)/watchlist/index.tsx, apps/mobile/src/features/watchlist/components/watchlist-state-view.tsx, apps/mobile/src/features/watchlist/components/watchlist-list.tsx, apps/mobile/src/components/pressable-row.tsx</files>
  <read_first>apps/mobile/src/app/(tabs)/watchlist/index.tsx, apps/mobile/src/components/list-row.tsx, apps/mobile/src/components/list-section.tsx, apps/mobile/src/components/pressable-row.tsx, apps/mobile/src/app/(modals)/profile.tsx, .planning/phases/02-watchlist-core/02-CONTEXT.md</read_first>
  <action>Replace the current placeholder watchlist tab with feature-driven UI. For signed-out users, render a centered CTA/button that explains login is required and opens the profile modal sign-in flow. For authenticated users, render a rich list row layout showing cover art, title name, and primary release date (best precision or TBA fallback). Add swipe-to-remove on mobile rows and wire removal to watchlist mutation hooks. Keep `addedAt` and platform list hidden in the at-a-glance row.</action>
  <verify>pnpm --filter mobile lint</verify>
  <acceptance_criteria>
    - `apps/mobile/src/app/(tabs)/watchlist/index.tsx` no longer contains `Array.from({ length: 50`.
    - Watchlist tab signed-out state contains CTA text indicating login is required.
    - `apps/mobile/src/features/watchlist/components/watchlist-list.tsx` contains swipe/remove interaction wiring.
    - Watchlist row rendering includes cover/title/release date fields and does not include `addedAt`.
  </acceptance_criteria>
  <done>Mobile watchlist tab matches Phase 2 shape and signed-out behavior decisions.</done>
</task>

<task type="code">
  <name>Integrate title-details header watchlist action with guest routing</name>
  <files>apps/mobile/src/app/titles/[titleId].tsx, apps/mobile/src/components/profile-header-button.tsx, apps/mobile/src/app/(modals)/profile.tsx</files>
  <read_first>apps/mobile/src/app/titles/[titleId].tsx, apps/mobile/src/auth/auth-provider.tsx, apps/mobile/src/app/_layout.tsx, apps/mobile/src/app/(modals)/profile.tsx, .planning/phases/02-watchlist-core/02-CONTEXT.md</read_first>
  <action>Add header-level watchlist action on title details. When authenticated, action toggles add/remove immediately with optimistic feedback and no confirm dialog. When signed out, action remains visible and opens the profile modal sign-in flow. After successful sign-in, keep users on their current screen and require manual retry per Phase 2 decision.</action>
  <verify>pnpm --filter mobile check-types</verify>
  <acceptance_criteria>
    - `apps/mobile/src/app/titles/[titleId].tsx` contains header watchlist action wiring.
    - Guest press path in title details routes to profile modal/sign-in flow.
    - Removal path contains no confirmation dialog logic.
  </acceptance_criteria>
  <done>Title details on mobile supports watchlist action behavior for authenticated and guest users.</done>
</task>

## Verification
- `pnpm --filter mobile lint`
- `pnpm --filter mobile check-types`
- Manual: signed-out watchlist tab CTA opens profile modal; signed-in user can add/remove from title details and swipe-remove in watchlist.

## Success Criteria
- [ ] Mobile users can view and manage watchlist entries from the decided entry points.
- [ ] Signed-out users are routed to profile sign-in flow from both watchlist tab and title-details header action.
- [ ] Watchlist row/card detail content follows the captured Phase 2 display rules.
